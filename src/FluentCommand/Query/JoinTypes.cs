namespace FluentCommand.Query;

/// <summary>
/// Query join types
/// </summary>
public enum JoinTypes
{
    /// <summary>The inner join tables</summary>
    Inner,
    /// <summary>The left outer join tables</summary>
    Left,
    /// <summary>The right outer join tables</summary>
    Right
}
